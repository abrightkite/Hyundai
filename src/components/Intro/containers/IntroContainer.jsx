import React from "react";
import { introData } from "../../../locale/localeData";
import Intro from "../Intro";

const IntroContainer = () => {
  return <Intro introData={introData} />;
};

export default IntroContainer;
